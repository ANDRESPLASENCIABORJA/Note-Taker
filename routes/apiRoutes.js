// Require the router and the db file
const router = require('express').Router();
const store = require('../db/notesData');





// ROUTING
// We have to make a post route for the notes so the user can type a note
